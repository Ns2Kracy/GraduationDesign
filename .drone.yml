kind: pipeline
type: exec
name: web

workspace:
  path: C:\Drone\builds\web-%RANDOM%\drone\src

platform:
  os: windows
  arch: amd64

trigger:
  branch:
    include:
      - main
      - dev
    event:
      - push
      - pull_request

steps:
  - name: lint
    commands:
      - pnpm install
      - pnpm run lint
      - pnpm run lint:fix
  - name: format
    commands:
      - pnpm run format
  - name: build
    commands:
      - pnpm run build

---
kind: pipeline
type: exec
name: server

workspace:
  path: C:\Drone\builds\server-%RANDOM%\drone\src

platform:
  os: windows
  arch: amd64

steps:
  - name: clippy
    commands:
      - cargo clippy -- -D warnings
      - cargo clippy --all-targets --all-features -- -D
      - cargo clippy --fix
  - name: fmt
    commands:
      - cargo fmt --all -- --check
      - cargo fmt --all
  - name: build
    commands:
      - cargo build --release
